# Copy to .env for `docker compose up`. Compose reads these automatically.
CRONOVA_ADMIN_USER=admin
CRONOVA_ADMIN_PASSWORD=change-me-please
# CRONOVA_SECURE_COOKIE=true   # enable when serving behind HTTPS
